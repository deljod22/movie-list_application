import React from 'react';

const FooterComponent = () => (
    <footer className="text-center mt-4 mb-2 text-muted">
        <small>Made with ❤️ using React & TMDb API</small>
    </footer>
);

export default FooterComponent;
